apiVersion: v1
kind: Service
metadata:
  name: life-tracker-backend-staging-service
spec:
  selector:
    app: life-tracker-backend-staging
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
